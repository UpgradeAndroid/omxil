The OpenMAX IL Bellagio package ver. 0.9.2 has been released. 
The main additions to this delivery are:

 - A new resource manager has been added. It is a generic resource manager
   used to implement the behavior needed for WaitForResoruces state
 - Removed all the components depending on external libraries. 
   They will be released separately as external components.

